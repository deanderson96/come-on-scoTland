document.addEventListener("DOMContentLoaded", () => {
  const holder = document.querySelector("#scotlandnt-timeline");
  if (!holder) return;

  holder.innerHTML = '<p class="timeline-fallback">Posts by ScotlandNT will load here.</p>';
});
